export const metadata = {
  title: "OGP短縮リンク作成",
  description: "画像付き短縮リンク"
};

export default function RootLayout({ children }) {
  return (
    <html lang="ja">
      <body>{children}</body>
    </html>
  );
}
